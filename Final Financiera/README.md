# App Cliente — Santander Consumer

## Base de datos (UNA SOLA para ambas apps)

Usa el archivo en la carpeta padre:

```
../supabase_completo.sql
```

**Solo ejecuta ESE archivo** en Supabase SQL Editor. No uses los otros SQL por separado.

## Login demo

| Campo | Valor |
|-------|-------|
| DNI | `45781290` |
| Password | `santander2026` |

(El email interno es `45781290@cliente.santander.pe`)

## Ejecutar app

```powershell
flutter pub get
flutter run
```
